{
  "name": "drawcirclegame",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild client/script.ts --bundle --outfile=client/dist/script.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.13.7"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "better-sqlite3": "^13.0.3",
    "esbuild": "^0.28.2",
    "prettier": "^3.9.7",
    "vitest": "^5.0.1",
    "wrangler": "^4.135.0"
  }
}
